import { Typography } from "@mui/material";
import React from "react";

const Users = () => {

    return (
        <div>
            <Typography variant="h1" >Users page</Typography>
            <Typography variant="p" >Find friends and view others' posts</Typography>
        </div>
    );
};

export default Users;